using System;
using Microsoft.Maui.Controls;

namespace amanda
{
    public partial class Financeiro : ContentPage
    {
        public Financeiro()
        {
           // InitializeComponent();

           
            //SalvarButton.Clicked += (sender, args) =>
            {
                
                //string despesas = DespesasEntry.Text;
               
                //await DisplayAlert("Salvo", "Dados salvos com sucesso!", "OK");
            };
        }
    }
}